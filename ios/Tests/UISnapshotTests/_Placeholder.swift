// Placeholder so SPM recognises the target directory. Real snapshot tests are
// iOS-only and arrive with the UI screen implementations. Each future test
// file should be guarded with `#if canImport(UIKit)` so `swift test` on macOS
// hosts stays green.
